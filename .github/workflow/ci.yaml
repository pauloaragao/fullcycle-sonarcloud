name: ci-sonarcloud
on: 
  pull_request:
    branches:
      - develop

jobs:
  run-ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@2
      - uses: actions/setup-go@2
        with:
          go-version: 1.15
      - run: go test -covereprofile=coverage.out